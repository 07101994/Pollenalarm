<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Pollenalarm.Frontend.Forms.Views.SettingsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:i18n="clr-namespace:Pollenalarm.Frontend.Forms.MarkupExtensions;assembly=Pollenalarm.Frontend.Forms"
    Title="Settings">
    <ScrollView>
        <StackLayout>
            <StackLayout.Padding>
                <OnPlatform
                    x:TypeArguments="Thickness"
                    Android="{StaticResource DefaultSicknessAndroid}"
                    WinPhone="{StaticResource DefaultSicknessWinPhone}"
                    iOS="{StaticResource DefaultSicknessiOS}" />
            </StackLayout.Padding>
            <Label FontAttributes="Bold" Text="{i18n:Translate Location}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Label
                    Grid.Column="0"
                    FontSize="Small"
                    Text="{i18n:Translate LocationSettingsText}"
                    VerticalOptions="Center" />
                <Switch Grid.Column="1" VerticalOptions="Center" />
            </Grid>

			<AbsoluteLayout>
				<Grid.Padding>
                    <OnPlatform
                        x:TypeArguments="Thickness"
                        Android="{StaticResource DefaultSicknessAndroid}"
                        WinPhone="{StaticResource DefaultSicknessWinPhone}"
                        iOS="15" />
                </Grid.Padding>
				
				<StackLayout>
                    
                    <Label FontAttributes="Bold" FontSize="16" Text="Langenfeld" />
	                <Label FontSize="12" Text="Pollution: High" Margin="0,-4,0,0"/>
	            </StackLayout>

				<Image 
					Source="Next"
					HeightRequest="15"
					AbsoluteLayout.LayoutBounds="1,0.5"
					AbsoluteLayout.LayoutFlags="PositionProportional"/>
			</AbsoluteLayout>  
        </StackLayout>
    </ScrollView>
</ContentPage>