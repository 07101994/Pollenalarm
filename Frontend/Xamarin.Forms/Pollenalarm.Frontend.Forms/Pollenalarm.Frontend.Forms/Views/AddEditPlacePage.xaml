<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Pollenalarm.Frontend.Forms.Views.AddEditPlacePage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <StackLayout>
        <StackLayout.Padding>
            <OnPlatform
                x:TypeArguments="Thickness"
                Android="{StaticResource DefaultSicknessAndroid}"
                WinPhone="{StaticResource DefaultSicknessWinPhone}"
                iOS="{StaticResource DefaultSicknessiOS}" />
        </StackLayout.Padding>
        <Entry Placeholder="Name" Text="{Binding PlaceName, Mode=TwoWay}" />
        <Entry Placeholder="Zip" Text="{Binding PlaceZip, Mode=TwoWay}" />
        <Button
            x:Name="AddButton"
            Command="{Binding AddEditPlaceCommand}"
            Text="Add" />
    </StackLayout>
</ContentPage>